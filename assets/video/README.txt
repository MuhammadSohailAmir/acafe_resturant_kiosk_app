Place the kiosk welcome background video here as:

    kiosk_intro.mp4

It will be played muted + looping as the background of the first (welcome)
screen. If the file is missing, the screen falls back to a static background
image (assets/image/splash_background.png), so the app still runs.

Recommended: a portrait (vertical) H.264 .mp4, no audio needed.
